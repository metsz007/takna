# Takna audit — 2026-07-07

Follow-up audit. Yesterday's report (2026-07-06.md) had all findings closed
across 16 cycles; this one carries forward the results of the cross-cutting
code review of `5189584..HEAD` run afterwards.

## Status snapshot

- **Tests:** 40/40 pass; `flutter analyze` clean.
- **Plans:** 01 done, 02 done (each with one manual device checkbox open).
- **Review:** 8 findings (6 correctness, 2 cleanup). All 6 correctness
  findings fixed and verified in beb65c1 (foreground action taps, re-enable on
  reschedule, reconcile serialization, offset-aware auto-disable, dismiss
  try/finally, cold-start fallback).

## Open findings

1. **Reuse:** the snooze-wins-over-next-occurrence rule is hand-copied in
   `_HomeList` (home_screen.dart) and the detail screen ("mirrors _HomeList"
   comment). Belongs as one helper next to `nextOccurrences` in
   `domain/recurrence.dart`, e.g. `effectiveNextFire(Reminder, DateTime)`.
   **Fix: small.**
2. **Simplification:** settings re-reads the two permissions
   `reliabilityProvider` already owns via its own lifecycle observer — double
   platform-channel probes per resume, two states that can disagree. Settings
   should watch the provider and keep only the battery read local. **Fix:
   small.**

## Manual checks

Both verified by the user on-device (2026-07-07):

- Plan 01: deny notifications on the emulator → warning card/banner; grant →
  green on resume. ✓
- Plan 02: device in a DST-observing zone reports the real IANA id. ✓

## Feature suggestion (`--feature`)

Nothing genuinely worth building emerged this pass — the review surfaced
integration bugs, not unmet user needs, and the app's v1 scope is otherwise
complete. Forcing a suggestion would violate the audit's own rule; revisit
after real usage feedback.

## Auto-cycles

- Cycle 17 (2026-07-07): picked finding 1 (`effectiveNextFire` extracted to domain/recurrence.dart; home + detail now share it) — **shipped**. Commits: 014fb63 (report), 9806ad1 (refactor). 45/45 tests, analyze clean.
- Cycle 29 (2026-07-07): picked plan 15 (vacation pause — single-row AppState table, scheduler floor max(now, pausedUntil) with post-pause pre-queueing, settings row + unmissable home banner with Resume; schema 7→8; builder fixed a rowid-alias bug by pinning id 0) — **shipped**. 96/96 tests, analyze clean. Manual device checks open. **Batch drained — plans 12-15 all shipped; loop ended.** Remaining backlog: 06/10 (supervised), 11 (assets), manual checks for 12-15.
- Cycle 28 (2026-07-07): picked plan 14 (alarm volume ramp — Kotlin-only, 0.4→1.0 over 45s in playAlarm, API 28+ guarded, ramp cancelled on stop/destroy/restart) — **shipped**. 92/92 tests, analyze clean, debug APK builds. Manual device checks open (ramp audibility is device-only).
- Cycle 27 (2026-07-07): picked plan 13 (dismiss challenge — nullable `challenge` column, seeded math generator, ring-screen Dismiss gated while all snooze paths stay ungated; schema 6→7; ring body made scrollable for the keyboard) — **shipped**. 92/92 tests, analyze clean. Manual device checks open.
- Cycle 26 (2026-07-07): plans 12-15 written from challenged feature ideas (commit c6b822d; rejected: interval-from-last-dose, NL quick add, Wear OS, med stock). Picked plan 12 (reliability report — Alarm history screen, pure `alarm_report.dart`, missed-alarm detection scoped to alarm-mode, no schema change) — **shipped**. 84/84 tests, analyze clean. Manual device checks open.
- Post-loop (2026-07-07): user verified all manual device checks for plans 03/04/05/07/08/09 on the emulator. Session fixes along the way: onboarding overflow on short screens, future-start recurring crash (rrule `after >= start` assert), stale last-fired provider cache, detail-page reload flash, and skip UX reworked to per-occurrence Skip/Undo pills (skip any of the next 5). Version now shown in settings (package_info_plus, build 1.0.0+2). Remaining: plans 06/10 (supervised builds), 11 (needs sound assets).
- Cycle 25 (2026-07-07): considered plan 11 (per-reminder sound) — **stopped for approval**. Approval gate challenged and confirmed the user is genuinely needed: the audio assets can't be verified on this host (a corrupt tone = silent alarm, undetectable by tests; iOS .caf/pbxproj unbuildable on Windows) and sourcing royalty-free audio is a licensing call. Plan stays `planned`; user should supply/approve 2-3 tone files, then build. Remaining plans 06 (nagging) and 10 (widget) were pre-flagged for supervised builds. **Loop ended — 6 of 9 plans shipped (03/04/05/07/08/09); backlog for the user: 11 (assets), 06 + 10 (supervised), plus per-plan manual device checks.**
- Cycle 24 (2026-07-07): picked plan 08 (tags + home filtering — nullable `tag` column, trim-to-null field in add/edit, chip row with stale-selection clamp; schema 5→6) — **shipped**. 72/72 tests, analyze clean. Manual device checks open.
- Cycle 23 (2026-07-07): picked plan 07 (skip next occurrence — `skippedDates` column filtered in `nextOccurrences`, skipNext/unskip + Undo snackbar on detail; schema 4→5) — **shipped**. 68/68 tests, analyze clean. Manual device check open. Pre-existing note: `detail_snooze_test.dart` prints a drift multiple-database warning (fixture quirk, not a failure).
- Cycle 22 (2026-07-07): picked plan 04 (alarm history — `fired_events` table with 90-day prune, capture in `handleNotificationAction` + alarm screen, "Last fired" line on detail; schema 3→4) — **shipped**. 60/60 tests, analyze clean. Manual device checks open.
- Cycle 21 (2026-07-07): picked plan 03 (backup/restore — JSON export via share sheet, atomic import via file picker, `importAll` + single reconcile). Approval gate challenged for the two new deps (share_plus, file_selector) and passed: standard pinned packages, plan-justified, user pre-approved via the loop; mitigation `flutter build apk --debug` ran green. — **shipped**. 87/87 tests, analyze clean. Manual device checks open.
- Cycle 20 (2026-07-07): picked plan 05 (ring-screen snooze presets — 5/10/30 chips on the alarm screen, big button keeps the saved default) — **shipped**. 49/49 tests, analyze clean. Manual device check open.
- Cycle 19 (2026-07-07): user requested 9 offline feature plans (03-11, commit 9e32940); picked plan 09 (duplicate reminder — `copyFromId` on the add screen via `/add?copy=`, Duplicate button on detail) — **shipped**. 46/46 tests, analyze clean. Manual device check open.
- Cycle 18 (2026-07-07): picked finding 2 (settings reads permission state via providers; new `batteryUnrestrictedProvider`; single reading path) — **shipped**. Commit: 78ce3bd. 45/45 tests, analyze clean. **All findings closed; backlog drained — loop ended.** Remaining for the user: the two manual device checks (plan 01 permission flow, plan 02 DST zone id).
